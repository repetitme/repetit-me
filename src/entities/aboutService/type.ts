export interface IAboutService {
    bonusPopup: {
        title: string
        text: string
        buttonText: string
        URL: string
        points: number
    }

    reviewPopup: {
        title: string
        text: string
        buttonText: string
    }
}